import sys

numerals = [
  (1000, 'M'),
  (500, 'D'),
  (100, 'C'),
  (50, 'L'),
  (10, 'X'),
  (5, 'V'),
  (1, 'I'),
]

def ator(i):
  s = ''
  for j in range(len(numerals)):
    n, c = numerals[j]
  return 'I'

for n in sys.argv[1:]:
  print(ator(n))